<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".MyOTApplication"
        android:usesCleartextTraffic="true"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyOT"
        tools:targetApi="31">
        <activity
            android:name=".memorybook.EditMemoryActivity"
            android:exported="false" />
        <activity
            android:name=".chatting.newchat.NewChatActivity"
            android:exported="false" /> <!-- 채팅 생성을 위한 액티비티 -->
        <activity
            android:name=".chatting.chatroom.ChatRoomActivity"
            android:exported="false" /> <!-- 채팅 생성을 위한 액티비티 -->
        <activity
            android:name=".write.WriteQuestionActivity"
            android:exported="false" />

        <activity
            android:name=".write.WriteFeedActivity"
            android:exported="false" />

        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>